# dsn: 'mysql:host=localhost;dbname=wordpress_trunk' - must be set a fake database name, cush as framework fill drop all tables in it.
#- Db
class_name: BackendTester
modules:
    enabled:
        - WebDriver
        - CommonHelper
    config:
      WebDriver:
         url: 'http://src.wordpress-develop.dev/'
         browser: firefox
         restart: false
         clear_cookies: false
         window_size: 1280x1024
         wait: 5
         capabilities:
             unexpectedAlertBehaviour: 'accept'
      Db:
        dsn: 'mysql:host=localhost;dbname=wordpress_develop'
        host: 'localhost'
        user: 'root'
        password: 'root'
        dump: tests/_data/atsumarov.sql
        populate: true